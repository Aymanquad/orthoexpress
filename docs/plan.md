PART D — Acceptance checklist
 Design tokens (color/spacing/type/shadow) defined once and reused, not redefined per page
 Icons + avatars present on Dashboard, Staff, Appointments
 Status values everywhere (appointments, prescriptions, staff active/inactive) render as colored badges, not plain text
 Login page no longer feels like a lone card on an empty gradient
 Dashboard has more than 3 static numbers — some activity/insight module
 Staff Access matrix includes Prescriptions + Demographics rows, enforced end-to-end
 Prescriptions and Demographics have audit fields (updatedBy/updatedAt) and staff UI surfaces "last updated by X"
 Patient-facing read-only page exists, scoped strictly to the logged-in patient's own records
 Every new screen (Prescriptions, Demographics, Patient portal) visually matches the Part A system — no one-off styling